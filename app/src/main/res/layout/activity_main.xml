<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="@dimen/main_screen_padding"
        android:orientation="vertical"
        android:gravity="center_horizontal">
        <ImageView
            android:layout_width="match_parent"
            android:layout_height="@dimen/main_screen_logo_height"
            android:contentDescription="@string/logo_desc"
            android:src="@drawable/logo"/>
        <LinearLayout
            android:id="@+id/start_button"
            android:layout_width="@dimen/start_btn_size"
            android:layout_height="@dimen/start_btn_size"
            android:layout_marginTop="@dimen/start_btn_to_margin"
            android:gravity="center"
            android:background="@drawable/item_circel_border_ripple">
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/start_string"
            android:textSize="@dimen/start_txt_size"
            />
        </LinearLayout>
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>